# Native runtime licenses

- `vrweb-wasm-runtime`: MIT, project source in this directory.
- `godot` 0.5.2 (`godot-rust`): MPL-2.0.
- `wasmtime` 46.0.1: Apache-2.0 WITH LLVM-exception.

Exact transitive versions are fixed by `Cargo.lock`; release packaging must include the license
texts produced from that lockfile.
